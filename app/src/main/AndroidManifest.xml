<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.riccardo.hermes">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo1"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registrazione" />
        <activity
            android:name=".Principale"
            android:label="@string/title_activity_principale"
            android:configChanges="keyboardHidden|screenSize|orientation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Profilo" />
        <activity android:name=".dettaglio_prodotto" />
        <activity android:name=".ModificaCliente" />
        <activity android:name=".modifica_prodotto" />
        <activity android:name=".dettaglio_acquistato" />
        <activity android:name=".CompletaAcquisto"></activity>
    </application>

</manifest>